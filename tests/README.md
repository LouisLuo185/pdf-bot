# Test Structure

Tests are intentionally separated from application code.

## Current layout

- `tests/unit`: fast isolated unit tests for pure logic and configuration

## Planned growth

- `tests/integration`: API routes, Supabase flows, and service interactions
- `tests/e2e`: user-facing upload and chat flows
